# DeepSeek HARNESS Quant · 依赖清单
# 安装：python -m pip install -r requirements.txt

# ---- 数据源 ----
baostock>=0.8.9            # 行情主源（稳）
akshare>=1.15.0            # 覆盖源（业绩快报/机构调研等）
tushare>=1.4.0             # 财报主源（ann_date → Point-in-Time，需 token）
requests>=2.28             # HTTP（Tushare 备用接口 / 质量备份 / 历史补拉）

# ---- 数据处理 ----
numpy>=1.26
pandas>=2.2
pyyaml>=6.0                # 读取 config/params.yaml
scipy>=1.10                # 统计（贝叶斯 Kelly / 时序因子显著性检验）
py7zr>=0.20                # 分钟数据 7z 增量解压

# ---- 可选 ----
matplotlib>=3.8            # 图表（validation/gen_backtest_charts.py）
